# 자동매매 딥러닝 플로우 (Deep Learning Studio 전략 기준)

## 0) 범위

이 문서는 `bitcoin-simulation` **클라이언트 자동매매** 흐름을 정리한 문서입니다.
전략은 지표 기반 필터 대신 **딥러닝 스튜디오에서 학습된 모델**을 불러와
해당 모델의 매수/매도 조건을 판단 기준으로 사용합니다.

- 대상: `AutoTradingDialog` + `App.jsx` 시간 스케줄러 + `autoTradeLogicDL.js` (신규)
- 비대상(별도): `automation_settings`/GitHub Actions 기반 봇 흐름

---

## 1) 진입점 및 스케줄

### 1-1. 사용자 설정

사용자는 `AutoTradingDialog`에서 아래 값을 설정합니다.

- 자동 매매 활성화 (`isEnabled`)
- 분석 대상 그룹 (`targetGroup`)
- 주문 방식 (`amountType`, `buyAmount`)
- **실행 시각** (`executionTime`: HH:mm 형식, 예: `15:20`): 해당 시각에 딱 한 번만 실행
- **딥러닝 모델 선택** (`selectedModelId`): 딥러닝 스튜디오에서 저장된 모델 목록 중 선택
- V-Martingale 옵션 (`useVMartingale`, `vMartingaleProfitCut`)

> 기존 전략 필터 (`useBB`, `useTrend`, `useRSI` 등)는 사용하지 않습니다.
> 모델 자체가 해당 조건들을 내포합니다.

활성화 시 KIS 로그인이 필요하며, 미로그인 상태에서는 활성화가 차단됩니다.

### 1-2. 시간 기반 스케줄러

`App.jsx`에서 앱 로드 후 `setInterval(60000)`로 1분마다 **현재 시각을 체크**합니다.
현재 시각(HH:mm)이 설정된 `executionTime`과 일치하면 `executeAutoTradeDL()`을 **단 한 번** 호출합니다.

- 매 분마다 매매를 실행하는 것이 아니라, **설정 시각에 정확히 1회** 실행
- 같은 날 이미 실행된 경우(`lastRunDate === today`) 재실행하지 않음
- 앱이 설정 시각 이후에 실행되더라도 당일 재실행되지 않음 (다음 날 해당 시각에 실행)

---

## 2) `executeAutoTradeDL()` 가드 조건

`src/lib/autoTradeLogicDL.js`의 `executeAutoTradeDL(isTest = false)`에서 아래 조건을 먼저 체크합니다.

1. (테스트 모드가 아니고) 자동매매 비활성화면 즉시 종료
2. KIS 로그인 미완료면 종료 + 로그 기록
3. **선택된 딥러닝 모델이 없으면 종료** + 로그 기록
4. 하루 1회 실행 원칙: `lastRunDate === today`면 종료
5. **현재 시각(HH:mm) === `executionTime` 일치 여부 확인**
   - 테스트 모드: 시각 무관 즉시 실행
   - 일반 모드: 현재 시각이 설정 시각과 일치할 때만 진행

실행 직전 `setLastRunDate(todayStr)`로 재진입을 방지합니다.

---

## 3) 딥러닝 모델 로드 (`loadDLModel`)

실제 매매 프로세스 시작 전, 선택된 모델을 메모리에 로드합니다.

- 딥러닝 스튜디오에서 저장된 모델 파일(`.json` + 가중치)을 `selectedModelId` 기준으로 로드
- 모델 메타데이터에서 아래 정보를 추출합니다:
  - **매수 조건**: 모델 출력값 중 BUY 클래스 확률 임계값 (`buyThreshold`)
  - **매도 조건**: 모델 출력값 중 SELL 클래스 확률 임계값 (`sellThreshold`)
  - **입력 피처 목록**: 모델 학습 시 사용한 지표 컬럼 리스트
  - **시퀀스 길이**: 모델이 참조하는 과거 봉 수 (`lookbackPeriod`)
- 로드 실패 시 전체 프로세스 중단 + 로그 기록

---

## 4) 실제 매매 프로세스 (`runAutoTradeDLProcess`)

### Step 1. 보유 종목 조회

- `getOverseasBalance(...)` 호출
- 보유 수량이 있는 종목만 추림
- `holdingTickers` 집합 생성

### Step 2. 매수 분석 대상 로드

`targetGroup`에 따라 대상 티커 목록을 구성합니다.

- `myholdings`
- `sp500`
- `qqq`
- `superinvestor`

이후 각 티커의 과거 데이터를 로드하고(`fetchStockHistory`), 모델의 **입력 피처 목록**에 맞춰 지표를 계산합니다.
(기존 `addDerivedData` 사용, 단 모델 메타데이터의 피처 목록 기준으로 필터링)

### Step 3. 딥러닝 매수 신호 스캔

- 병렬 처리 단위: `CHUNK_SIZE = 5`
- 각 티커에 대해:
  1. 최근 `lookbackPeriod`개 봉 데이터를 입력 피처 기준으로 슬라이싱
  2. `model.predict(inputTensor)` 호출
  3. BUY 클래스 확률 >= `buyThreshold` 이면 `buyList`에 추가
- 기본적으로 보유 종목은 매수 대상에서 제외
- 단, `useVMartingale=true`이면 보유 종목의 추가 매수 허용

### Step 4. 딥러닝 매도 신호 스캔 (보유 종목)

- 보유 종목 각각에 대해:
  1. 최근 `lookbackPeriod`개 봉 데이터 슬라이싱
  2. `model.predict(inputTensor)` 호출
  3. SELL 클래스 확률 >= `sellThreshold` 이면 `sellList` 후보
- 앞 단계에서 BUY로 잡힌 종목은 매도에서 제외
- V-Martingale 활성화 시 최소 수익률(`vMartingaleProfitCut`) 미달이면 매도 보류

### Step 5. 매도 주문 실행 (선행)

- 각 `sellList` 항목에 대해 현재가/거래소 조회
- `sellOverseasStock(...)` 호출
- 성공 시 주문 추적(`addPendingOrder`) 등록

### Step 6. 매수 주문 실행

- `amountType`이 `quantity`면 수량 직접 사용
- `amount`면 현재가 기반 환산 수량 사용
- V-Martingale 대상이면 레벨 배수 적용
- `buyOverseasStock(...)` 호출
- 성공 시 주문 추적(`addPendingOrder`) 등록

### Step 7. 체결 모니터링

- 테스트 모드가 아니고 모니터링 미실행 상태면
- `startSettlementMonitoring(kisAuth, 600000)` 시작 (10분 간격)

---

## 5) 테스트 모드

`AutoTradingDialog`의 `⚡ 즉시 실행 (테스트)` 버튼은 `executeAutoTradeDL(true)`를 호출합니다.

- 모델 로드 및 신호 분석은 동일하게 수행
- 실제 주문은 수량 `0`으로 처리하여 체결 방지
- 모델 예측 결과 및 신호/흐름 검증 용도

---

## 6) 상태 저장 포인트 (Zustand)

- 설정: `autoTradeSettings` (`selectedModelId` 포함)
- 실행 이력: `autoTradeStatus.lastRunDate`
- 로그: `autoTradeStatus.logs`
- 주요 액션: `setAutoTradeSettings`, `addAutoTradeLog`, `setLastRunDate`

---

## 7) 기존 전략과의 차이점 요약

| 항목 | 기존 전략 | 딥러닝 전략 |
|------|-----------|-------------|
| 신호 판단 | `analyzeSignal()` + 지표 필터 조합 | 모델 `predict()` 확률값 기준 |
| 매수 조건 | BB, Trend, RSI 등 개별 필터 ON/OFF | `buyThreshold` 초과 여부 |
| 매도 조건 | 동일 지표 역방향 신호 | `sellThreshold` 초과 여부 |
| 파라미터 설정 | 필터 체크박스 다수 | 모델 선택 1개 + 임계값 |
| 피처 계산 | 고정 지표 세트 | 모델 메타데이터 기반 동적 피처 |

---

## 8) 한 줄 요약

자동매매 활성화 후 클라이언트는 **1분마다 현재 시각을 확인**하여,
설정된 실행 시각이 되면 **하루 1회** `딥러닝 모델 로드 → 보유조회 → 모델 예측 기반 매수/매도 신호 분석 → 매도 선행 실행 → 매수 실행 → 체결 모니터링` 순으로 동작합니다.
