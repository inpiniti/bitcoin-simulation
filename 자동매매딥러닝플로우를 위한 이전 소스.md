# 자동매매 딥러닝 플로우를 위한 백엔드 이전 소스 정리

## 개요

자동매매 딥러닝 플로우를 클라이언트(`bitcoin-simulation`)에서
백엔드(`bitcoin-ai-backend`, Python/FastAPI)로 이전하기 위해
옮겨야 할 소스와 새로 구현해야 할 내용을 정리합니다.

---

## 1. KIS API 연동

### 이전 소스
- `bitcoin-simulation/src/lib/kisApi.js`

### 이전 내용
| 기능 | 현재 함수명 | 백엔드 구현 위치 |
|------|------------|----------------|
| 토큰 발급 / 자동 갱신 | `getKisToken()` | `services/kis_service.py` |
| 해외 잔고 조회 | `getOverseasBalance()` | `services/kis_service.py` |
| 현재가 조회 | `getOverseasCurrentPrice()` | `services/kis_service.py` |
| 거래소 조회 | `getExchangeCode()` | `services/kis_service.py` |
| 매수 주문 | `buyOverseasStock()` | `services/kis_service.py` |
| 매도 주문 | `sellOverseasStock()` | `services/kis_service.py` |
| 체결 내역 조회 | `getOrderStatus()` | `services/kis_service.py` |

### 주요 고려사항
- KIS API Key, Secret, 계좌번호는 백엔드 **환경변수**로 관리
- 토큰 만료 시 자동 재발급 로직 포함 필요
- 라우터: `routers/kis.py` 신규 생성

---

## 2. 주가 데이터 로드

### 이전 소스
- `bitcoin-simulation/src/lib/api.js` 내 `fetchStockHistory()` 관련 함수

### 이전 내용
- 백엔드에 `yahoo_service.py`가 이미 존재하므로 **확장** 방식으로 이전
- 티커 목록을 받아 OHLCV 과거 데이터를 반환하는 엔드포인트 추가

| 기능 | 백엔드 구현 위치 |
|------|----------------|
| 단일 티커 과거 데이터 조회 | `services/yahoo_service.py` 확장 |
| 복수 티커 배치 조회 | `services/yahoo_service.py` 확장 |

### 주요 고려사항
- `lookbackPeriod` (모델이 필요로 하는 과거 봉 수)만큼 데이터 확보 필요
- 라우터: `routers/stock_data.py` 신규 생성 또는 기존 라우터 확장

---

## 3. 기술적 지표 계산

### 이전 소스
- `bitcoin-simulation/src/lib/dataProcessor.js` 내 `addDerivedData()`

### 이전 내용
- Python의 `pandas`, `ta` 라이브러리로 재구현
- 모델 메타데이터의 **피처 목록 기준**으로 필요한 지표만 동적 계산

| 지표 | 현재 구현 | 백엔드 라이브러리 |
|------|----------|-----------------|
| 볼린저 밴드 (BB) | JS 직접 계산 | `ta.volatility.BollingerBands` |
| RSI | JS 직접 계산 | `ta.momentum.RSIIndicator` |
| 이동평균 (MA) | JS 직접 계산 | `pandas.DataFrame.rolling` |
| 거래량 지표 | JS 직접 계산 | `ta.volume.*` |

- 구현 위치: `services/indicator_service.py` 신규 생성

---

## 4. 딥러닝 모델 로드 & 추론

### 이전 소스
- `bitcoin-simulation/src/lib/mlProcessor.js`
- 딥러닝 스튜디오에서 저장된 모델 파일 (`.json` + 가중치, TF.js 형식)

### 이전 내용
- 브라우저용 TF.js → 서버용 Python TensorFlow / ONNX Runtime으로 전환

| 기능 | 현재 | 백엔드 |
|------|------|--------|
| 모델 파일 로드 | `tf.loadLayersModel()` (TF.js) | `tensorflow.keras.models.load_model()` 또는 `onnxruntime.InferenceSession()` |
| 입력 텐서 생성 | JS Float32Array | `numpy.ndarray` |
| 예측 (predict) | `model.predict()` | `model.predict()` 또는 `session.run()` |
| BUY/SELL 확률 추출 | JS 배열 슬라이싱 | numpy 슬라이싱 |

### 모델 파일 저장 위치 옵션
- **Supabase Storage**: 이미 Supabase 연동 중이므로 권장
- **백엔드 로컬 `/models` 디렉토리**: 현재 `bitcoin-ai-backend/models/` 폴더 존재, 소규모 시 가능

### 주요 고려사항
- TF.js 모델을 Python에서 직접 로드하려면 **SavedModel 또는 ONNX 포맷으로 변환** 필요
- 구현 위치: `services/dl_model_service.py` 신규 생성
- 라우터: `routers/auto_trade.py` 신규 생성

---

## 5. 티커 그룹 데이터

### 이전 소스
- `bitcoin-simulation/src/lib/sp500Data.js`
- `bitcoin-simulation/src/lib/qqqData.js`

### 이전 내용
- 각 그룹별 티커 목록을 백엔드에서 관리

| 그룹 | 현재 소스 | 백엔드 구현 |
|------|----------|------------|
| S&P 500 | `sp500Data.js` (JS 배열) | `data/sp500_tickers.json` 또는 DB 테이블 |
| QQQ | `qqqData.js` (JS 배열) | `data/qqq_tickers.json` 또는 DB 테이블 |
| superinvestor | JS 배열 또는 API | 동일 방식 |
| myholdings | KIS 잔고 조회 결과 | KIS API 잔고 조회로 대체 |

---

## 6. 자동매매 설정 & 실행 로그 저장

### 이전 소스
- Zustand store (`autoTradeSettings`, `autoTradeStatus`)
- `bitcoin-simulation/src/lib/tradeHistoryService.js`

### 이전 내용
- 브라우저 메모리(Zustand) → Supabase DB 테이블로 대체

| 데이터 | 현재 저장 | 백엔드 저장 |
|--------|----------|------------|
| 자동매매 설정 | Zustand `autoTradeSettings` | Supabase `auto_trade_settings` 테이블 |
| 마지막 실행일 | Zustand `lastRunDate` | Supabase `auto_trade_settings.last_run_date` |
| 실행 로그 | Zustand `logs` | Supabase `auto_trade_logs` 테이블 |
| 주문 추적 | `orderTracker.js` | Supabase `pending_orders` 테이블 |

- 이미 `services/supabase_service.py` 존재하므로 테이블 추가 및 CRUD 메서드 확장
- 프론트에서 로그 조회를 위한 조회 API 엔드포인트도 함께 추가

---

## 7. 스케줄러

### 이전 소스
- `bitcoin-simulation/src/App.jsx` 내 `setInterval` 기반 시각 체크

### 이전 내용 (옵션 중 선택)

**옵션 A. Vercel Cron → 백엔드 호출**
- `vercel.json`에 cron 설정 추가
- 설정 시각에 백엔드 `/api/auto-trade/run` 엔드포인트를 HTTP 호출
- 백엔드는 단순히 호출을 받아 플로우 실행

**옵션 B. 백엔드 자체 스케줄러 (권장)**
- `APScheduler` 라이브러리로 백엔드 내부 크론 구현
- Docker로 상시 실행 중이므로 별도 외부 트리거 불필요
- DB의 `executionTime` 설정값을 읽어 동적으로 스케줄 등록/변경
- 구현 위치: `scheduler.py` 신규 생성 (FastAPI 앱 시작 시 함께 실행)

---

## 8. 신규 생성이 필요한 백엔드 파일 목록

```
bitcoin-ai-backend/
├── routers/
│   ├── kis.py               # KIS API 엔드포인트
│   ├── auto_trade.py        # 자동매매 실행 / 설정 / 로그 엔드포인트
│   └── stock_data.py        # 주가 데이터 조회 엔드포인트 (기존 확장)
├── services/
│   ├── kis_service.py       # KIS API 연동 로직
│   ├── indicator_service.py # 기술적 지표 계산
│   ├── dl_model_service.py  # 딥러닝 모델 로드 & 추론
│   └── auto_trade_service.py# 자동매매 플로우 전체 오케스트레이션
├── data/
│   ├── sp500_tickers.json   # S&P500 티커 목록
│   └── qqq_tickers.json     # QQQ 티커 목록
└── scheduler.py             # APScheduler 기반 크론 (옵션 B 선택 시)
```

---

## 9. 이전 우선순위 제안

| 순서 | 항목 | 이유 |
|------|------|------|
| 1 | KIS API 연동 | 매매 실행의 핵심, 없으면 나머지 의미 없음 |
| 2 | 주가 데이터 로드 | 지표 계산 및 모델 추론의 선행 조건 |
| 3 | 지표 계산 | 모델 입력 피처 생성 |
| 4 | 딥러닝 모델 추론 | 신호 판단 핵심 (모델 포맷 변환 포함) |
| 5 | 설정 / 로그 DB 저장 | 운영 안정성 |
| 6 | 스케줄러 | 전체 연결 후 마지막 조립 |
