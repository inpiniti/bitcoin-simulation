# 자동매매 플로우 (Simulation Client 기준)

## 0) 범위

이 문서는 `bitcoin-simulation` **클라이언트 자동매매** 흐름을 정리한 문서입니다.

- 대상: `AutoTradingDialog` + `App.jsx` 1분 스케줄러 + `autoTradeLogic.js`
- 비대상(별도): `automation_settings`/GitHub Actions 기반 봇 흐름

---

## 1) 진입점 및 스케줄

### 1-1. 사용자 설정

사용자는 `AutoTradingDialog`에서 아래 값을 설정합니다.

- 자동 매매 활성화(`isEnabled`)
- 분석 대상 그룹(`targetGroup`)
- 주문 방식(`amountType`, `buyAmount`)
- 실행 시점(`executionTimeMinutes`: 장 마감 N분 전)
- 전략 필터(`useBB`, `useTrend`, `useTrend20`, `useRSI`, `useVolumeFilter`)
- V-Martingale 옵션(`useVMartingale`, `vMartingaleProfitCut`)

활성화 시 KIS 로그인이 필요하며, 미로그인 상태에서는 활성화가 차단됩니다.

### 1-2. 1분 스케줄러

`App.jsx`에서 앱 로드 후 `setInterval(60000)`로 1분마다 `executeAutoTrade()`를 호출합니다.

---

## 2) `executeAutoTrade()` 가드 조건

`src/lib/autoTradeLogic.js`의 `executeAutoTrade(isTest = false)`에서 아래 조건을 먼저 체크합니다.

1. (테스트 모드가 아니고) 자동매매 비활성화면 즉시 종료
2. KIS 로그인 미완료면 종료 + 로그 기록
3. 하루 1회 실행 원칙: `lastRunDate === today`면 종료
4. 장 마감까지 남은 시간 계산: `getMinutesUntilClose()`
5. 실행 조건 충족 시에만 진행
	- 테스트 모드: 즉시 실행
	- 일반 모드: `minutesLeft <= executionTimeMinutes` 이고 `minutesLeft > 1`

실행 직전 `setLastRunDate(todayStr)`로 재진입을 방지합니다.

---

## 3) 실제 매매 프로세스 (`runAutoTradeProcess`)

### Step 1. 보유 종목 조회

- `getOverseasBalance(...)` 호출
- 보유 수량이 있는 종목만 추림
- `holdingTickers` 집합 생성

### Step 2. 매수 분석 대상 로드

`targetGroup`에 따라 대상 티커 목록을 구성합니다.

- `myholdings`
- `sp500`
- `qqq`
- `superinvestor`

이후 각 티커의 과거 데이터를 로드하고(`fetchStockHistory`), `addDerivedData`로 지표를 계산합니다.

### Step 3. 매수 신호 스캔

- 병렬 처리 단위: `CHUNK_SIZE = 5`
- `analyzeSignal(data, autoTradeStrategyOptions)` 호출
- BUY 신호면 `buyList`에 추가
- 기본적으로 보유 종목은 매수 대상에서 제외
- 단, `useVMartingale=true`이면 보유 종목의 추가 매수 허용

### Step 4. 매도 신호 스캔 (보유 종목)

- 보유 종목 각각에 대해 `analyzeSignal` 수행
- SELL이면 `sellList` 후보
- 단, 앞 단계 BUY로 잡힌 종목은 매도에서 제외
- V-Martingale 활성화 시 최소 수익률(`vMartingaleProfitCut`) 미달이면 매도 보류

### Step 5. 매도 주문 실행 (선행)

- 각 `sellList` 항목에 대해 현재가/거래소 조회
- `sellOverseasStock(...)` 호출
- 성공 시 주문 추적(`addPendingOrder`) 등록

### Step 6. 매수 주문 실행

- `amountType`이 `quantity`면 수량 직접 사용
- `amount`면 현재가 기반 환산 수량 사용
- V-Martingale 대상이면 레벨 배수 적용
- `buyOverseasStock(...)` 호출
- 성공 시 주문 추적(`addPendingOrder`) 등록

### Step 7. 체결 모니터링

- 테스트 모드가 아니고 모니터링 미실행 상태면
- `startSettlementMonitoring(kisAuth, 600000)` 시작 (10분 간격)

---

## 4) 테스트 모드

`AutoTradingDialog`의 `⚡ 즉시 실행 (테스트)` 버튼은 `executeAutoTrade(true)`를 호출합니다.

- 분석 로직은 동일하게 수행
- 실제 주문은 수량 `0`으로 처리하여 체결 방지
- 로직/신호/흐름 검증 용도

---

## 5) 상태 저장 포인트 (Zustand)

- 설정: `autoTradeSettings`
- 실행 이력: `autoTradeStatus.lastRunDate`
- 로그: `autoTradeStatus.logs`
- 주요 액션: `setAutoTradeSettings`, `addAutoTradeLog`, `setLastRunDate`

---

## 6) 한 줄 요약

자동매매 활성화 후 클라이언트는 **1분마다 장 마감 임박 여부를 검사**하고,
조건 충족 시 **하루 1회** `보유조회 → 매수/매도 신호 분석 → 매도 선행 실행 → 매수 실행 → 체결 모니터링` 순으로 동작합니다.

